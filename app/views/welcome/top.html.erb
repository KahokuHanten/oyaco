<style type="text/css">

body {
  background-color: #F5DEB3;
}

</style>
<div class="center hero-unit">
<% @holidays.each do |holiday| %>
  <p class="lead">
    <h1>
    ★<%= holiday.holiday_date.strftime('%Y年%-m月%e日') %>は<%= holiday.holiday_name %>です。★
  </h1>
    <h3>こんなプレゼントはどうですか？</h3>
  </p>
  <table class="table">
    <tbody>
      <% @items = RakutenWebService::Ichiba::Item.search(:keyword => holiday.holiday_name) %>
      <% @items.first(3).each do |item| %>
        <tr>
          <td><img src="<%= item['mediumImageUrls'][0]['imageUrl'] %>"></td>
          <td><a href="<%= item['affiliateUrl'] %>" target="_blank"><h3><%= item.name %></h3></a></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <p>
    <h3>こんな言葉を贈ってはどうですか？</h3>
  </p>

  <p>
    <h3> おじいちゃん、いつも、ありがとう！！！！！！！</h3>
  </p>
  
  <HR>
<% end %>


<!-- テスト -->

<p>
<h3><%= @pref_name %>は<%= @message %></h3>
</p>
